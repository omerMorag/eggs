import HashRedirect from "@/components/shell/HashRedirect";

/** נתיב ישן — מפנה אל האזור "איפה כדאי לעשות?" בתוך ה-App Shell החדש (/#where-to-go). */
export default function WhereToGoRedirectPage() {
  return <HashRedirect hash="#where-to-go" />;
}
